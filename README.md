# Offer Island · 求职小岛

本地优先的求职工作台，支持 Agent 接入与 macOS 桌面小岛。**v0.3.0**。

奶油纸色、手帐式卡片和彩色阶段标签。把投递、日程和面试经验留在自己的设备上。

## 现在能用什么

- 职位手动增删改、搜索筛选；可点击彩色状态标签，保留招聘平台原始状态和来源。
- 月历、今日待办、会议链接、完成标记。
- 岗位行内打开的面经卡片：面试轮次、问题、复盘、下次准备。
- JSON / CSV 导入收件箱、预览新增与更新、逐条核对、去重、全量导出。
- 文本草稿：提取明确标注的公司、职位、状态和投递日期，未识别字段留空，核对后保存。
- macOS 截图 OCR：使用 Apple Vision 在本机提取文字，再进入投递草稿核对；不内置大模型自动理解任意招聘页面。
- 最近 20 次操作可撤销、最近 500 条修改历史、跨窗口版本冲突保护。
- MCP：读取投递 / 今日安排、写入投递 / 日程、预览 / 提交导入、撤销。
- macOS 顶部悬浮小岛：可爱木鱼、合成敲击音、待办轮播、悬停暂停、30 分钟内临近安排优先固定、倒计时、免打扰、隐藏和展开。

## 开始使用

### 网页本地模式

无需依赖，使用静态服务器：

```sh
python3 -m http.server 8780 --bind 127.0.0.1 --directory web
```

打开 http://127.0.0.1:8780/ 。`?demo` 是隔离的虚构示例。这个模式使用浏览器存储；没有桌面 OCR 或跨设备同步。浏览器打开的小岛是普通弹出窗口，不会置顶。

### SQLite + Agent 模式

需要 Node.js 24 或更高版本：

```sh
npm ci
npm start
```

打开 http://127.0.0.1:8780/ 。页面通过本地服务保存到 SQLite，MCP 和桌面小岛使用相同数据。默认数据库：

```text
~/Library/Application Support/Offer Island/offer.sqlite
```

可用 `OFFER_DB` 更改数据库位置，`OFFER_PORT` 更改 Web 服务端口。此服务仅绑定 loopback，并检查 Host、Origin 和请求标识，不用于公网部署。

### macOS 应用

需要 macOS、Xcode Command Line Tools、Node.js 24+：

```sh
npm run build:mac
open "dist/Offer Island.app"
```

构建会把本机架构的 Node 运行时放入应用。启动应用后自动启动本地 SQLite 服务（18783 端口），显示工作台和小岛。菜单栏 ✳ 可显示 / 隐藏小岛、回到屏幕顶部和退出；退出时停止自有服务。

这是本地 ad-hoc 签名构建，**未做 Apple Developer ID 签名、公证或 App Store 发布**。当前构建基于本机架构 / SDK，未宣称支持所有旧版 macOS 或 Intel Mac。

桌面和 Netlify 网站不会自动共享数据。在网站“导出全部”，再到桌面“导入收件箱”核对并导入。原始网页 localStorage 不会被读取或上传。

## Agent 配置

见 [MCP 接入](docs/MCP.md)。MCP 使用 stdio，不依赖网页是否打开。Agent 和人工写入共用 `web/core/model.mjs` 验证与迁移规则。并发写入必须携带最近读取的 revision。

## 数据与备份

网页旧版存储会在第一次打开时迁移，旧键保持不动。导入默认只新增；允许覆盖已有字段时需勾选选项。日程必须有明确日期，无日期的面试通知留在职位的下一步中。

全量备份包含投递、日程、面试手记和历史。恢复通过导入合并，不清空现有记录；撤销栈不导出。清空浏览器存储会丢失未备份的数据。

小岛提供可见倒计时，不是系统级定时通知；关闭应用后不会弹提醒。无云同步、邮件账号接入、开机自启、自动更新或内置聊天模型。

## 开发与验证

```sh
npm test
```

测试覆盖字段验证、备份往返、旧数据迁移、导入去重、删除撤销、SQLite 并发保护和真实 MCP 协议握手 / 调用。

```text
web/         主界面、小岛、共享数据模型与浏览器存储
server/      SQLite 存储、loopback HTTP、MCP stdio
 desktop/    Swift AppKit + WebKit 宿主与 Apple Vision OCR
scripts/     本机 macOS 构建
 tests/      Node 测试
 docs/       接入说明与版本范围
```

公开源码只包含虚构演示数据。不提交真实投递、简历、截图、数据库或凭证。MIT License。

### 桌面入口调整
网页不再提供浏览器小岛入口。桌面应用启动后仅显示小岛和菜单栏；点击小岛右上角 ↗ 或菜单栏“打开网页版”，在默认浏览器打开本机工作台。内部服务随应用自动启动，网页与小岛共用 SQLite。此本机工作台并非 Netlify 公网页面；账号和云同步尚未接入。
